
procedure Hello is
   -- TODO: Fix parser so it can handle an empty declarative part.
   I : Integer;
begin
   null;
end;
-- TODO: Fix parser so it can handle an identifier after the 'end' token.
--       Update semantic analyzer to check correctness of that identifier.

