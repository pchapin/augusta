BEGIN
   IF X + Y THEN
      WHILE X + Y LOOP
         IF X + Y THEN
            A := B + C;
         END IF;
      END LOOP;
   END IF;

   whilE X + Y looP
      whilE X + Y looP
         A := B + C;
      enD looP;
   enD looP;

   If X + Y Then
      Null;
   Elsif X + Y Then
      While X + Y Loop
         If X + Y Then
           A := B + C;
         End If;
      End Loop;
   End If;
END
